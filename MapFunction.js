// In JavaScript, the map() function is an array method that 
//allows you to create a new array by applying a transformation to
// each element of an existing array. It iterates through the 
//elements of the array, calls a provided function on each element,
// and returns a new array with the results of the function calls.
// The original array remains unchanged.

/* const numbers = [1, 2, 3, 4, 5];

// Map each element to its square
const squaredNumbers = numbers.map(function(number) {
  return number * number;
});

console.log(squaredNumbers); */