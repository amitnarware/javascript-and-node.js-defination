// reverse bhopal

/*function reverseString(string){
    let reversed = ""
    for(let i = string.length - 1; i >= 0; i-- ){
        reversed += string.charAt(i)
    }
    return reversed;
}
    let string = "Bhopal";
    let reversedString = reverseString(string);
    console.log(reversedString); */

   /* function reverseString(string) {
        let reversed = "";
        for (let i = string.length - 1; i >= 0; i--) {
          reversed += string.charAt(i);
        }
        return reversed;
      }
      
      let String = "Delhi";
      let reversedString = reverseString(String);
      console.log(reversedString); */

      /*function reverseString(string){
        let reversed = "";
        for (let i = string.length -1; i >= 0; i--){
            reversed += string.charAt(i);
        }
        return reversed
      }
      let string = 'Bangalore';
      let reversedString = reverseString(string);
      console.log(reversedString); */

      /*let string = 'bhopal';
      let new_string = "";
      
      for (let i = string.length - 1; i >= 0; i--) {
        new_string = new_string + string[i];
      }
      
      console.log(new_string); */
       /*function findUniqueValues(arr) {
        const uniqueValues = [];
      
        for (let i = 0; i < arr.length; i++) {
          let isUnique = true;
      
          // Check if the current element already exists in the uniqueValues array
          for (let j = 0; j < uniqueValues.length; j++) {
            if (arr[i] === uniqueValues[j]) {
              isUnique = false;
              break;
            }
          }
      
          // If the current element is unique, add it to the uniqueValues array
          if (isUnique) {
            uniqueValues.push(arr[i]);
          }
        }
      
        return uniqueValues;
      }
      
      // Example usage
      const inputArray = [1, 2, 2, 3, 4, 4, 5, 5, 5];
      const uniqueArray = findUniqueValues(inputArray);
      console.log("Unique Values:", uniqueArray); // Output: [1, 2, 3, 4, 5] */

   /*let string = "bhopal";
   reversedString ="";

   for (let i = string.length-1; i >= 0; i--){
    reversedString += string[i];
   }
      
 console.log(reversedString); */

 /*let string = "Delhi";
 reversedString= "";
 for (let i = string.length-1;  i >= 0; i--){
  reversedString += string[i];
 }
  console.log(reversedString);  */ 
